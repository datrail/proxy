# Runtime. The image installs this file and nothing else.
fastmcp>=3.4,<4
httpx>=0.27
pyyaml>=6.0
uvicorn[standard]>=0.27

# The test suite runs against the versions above rather than a resolved-later
# set, so it is the runtime file that carries it.
pytest>=8.0
pytest-asyncio>=0.24
