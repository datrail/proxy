[pytest]
asyncio_mode = strict
testpaths = tests
# Without this, only `python -m pytest` works — that form puts the working
# directory on sys.path and a bare `pytest` does not, so the first command a
# contributor tries fails to import the package under test.
pythonpath = .
